export type Move = { key: number; name: string; locked: boolean };
export type PresetKey = "Boxing" | "Kickboxing" | "Muay Thai";
export type GenerationSettings = { min: number; max: number; bias: number; lengthVariance: number };
