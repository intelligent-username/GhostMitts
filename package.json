{
  "name": "ghostmitts",
  "version": "0.1.0",
  "description": "Audio-guided combat sports combinations trainer for Boxing, Kickboxing, Muay Thai, MMA, and Wrestling.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "NODE_ENV=production bun src/index.ts",
    "deploy": "bunx wrangler deploy --config worker/wrangler.toml",
    "preview:worker": "bunx wrangler dev --config worker/wrangler.toml",
    "api:dev": "bunx wrangler dev --config worker/wrangler.toml",
    "api:deploy": "bunx wrangler deploy --config worker/wrangler.toml",
    "api:tail": "bunx wrangler tail --config worker/wrangler.toml"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19",
    "@tursodatabase/serverless": "^1.1.3",
    "wrangler": "^4.72.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/bun": "latest",
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^7.0.0"
  }
}
